local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local correctKey = "Sown123" -- Key mà bạn muốn người dùng nhập

-- Tạo cửa sổ nhập key
local KeyWindow = Rayfield:CreateWindow({
    Name = "Nhập Key để tiếp tục",
    LoadingTitle = "Đang tải hệ thống",
    LoadingSubtitle = "Vui lòng nhập key",
    Theme = "Default",
    ConfigurationSaving = {
        Enabled = true, -- Tùy chọn này không ảnh hưởng đến lưu key thủ công
        FolderName = nil,
        FileName = "Pig's food hub"
    },
    KeySystem = false -- Không sử dụng KeySystem của Rayfield
})

-- Tạo một tab nhập key
local KeyTab = KeyWindow:CreateTab("Nhập Key")

-- Tạo một biến để lưu trạng thái nhập key
local isKeyValid = false

KeyTab:CreateInput({
    Name = "Nhập Key:",
    PlaceholderText = "Nhập key của bạn...",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        if Value == correctKey then
            isKeyValid = true
            Rayfield:Notify({
                Title = "Key hợp lệ!",
                Content = "Đang mở phần chính...",
                Duration = 3,
                Image = nil,
                Actions = {
                    Ignore = {
                        Name = "OK",
                        Callback = function() end
                    }
                }
            })
            
            -- Xóa KeyWindow và hiển thị MainWindow sau 0.1 giây để đảm bảo
            wait(0.1) -- Đảm bảo rằng KeyWindow được xóa trước khi tạo MainWindow
            KeyWindow:Destroy()  -- Xóa cửa sổ nhập key
            
            createMainWindow() -- Gọi hàm tạo giao diện chính
        else
            Rayfield:Notify({
                Title = "Key không hợp lệ!",
                Content = "Vui lòng thử lại.",
                Duration = 3,
                Image = nil,
                Actions = {
                    Ignore = {
                        Name = "OK",
                        Callback = function() end
                    }
                }
            })
        end
    end
})

-- Hàm tạo giao diện chính
function createMainWindow()
    local MainWindow = Rayfield:CreateWindow({
        Name = "Nồi cám hợi🤑",
        Icon = 0,
        LoadingTitle = "《《《 ĐANG LOAD 》》》",
        LoadingSubtitle = "script beta by S",
        Theme = "Default",
        ConfigurationSaving = {
            Enabled = false, -- Tắt lưu trữ dữ liệu của MainWindow
            FolderName = nil,
            FileName = "Pig's food hub"
        },
        Discord = {
            Enabled = false,
            Invite = "noinvitelink",
            RememberJoins = true
        }
    })

    local MainTab = MainWindow:CreateTab("local player🔥")

    -- Thêm Toggle Fly
    local isFlying = false

    local function toggleFly(state)
        local character = game.Players.LocalPlayer.Character or game.Players.LocalPlayer.CharacterAdded:Wait()
        local humanoid = character:FindFirstChild("Humanoid")
        local primaryPart = character.PrimaryPart

        if state then
            humanoid.PlatformStand = true
            local flyPart = Instance.new("BodyVelocity", primaryPart)
            flyPart.Velocity = Vector3.new(0, 50, 0)
            flyPart.MaxForce = Vector3.new(100000, 100000, 100000)
            flyPart.Name = "FlyVelocity"
        else
            local flyVelocity = primaryPart:FindFirstChild("FlyVelocity")
            if flyVelocity then
                flyVelocity:Destroy()
            end
            humanoid.PlatformStand = false
        end
    end

    MainTab:CreateToggle({
        Name = "Enable/Disable Fly",
        CurrentValue = false,
        Flag = "FlyToggle",
        Callback = function(Value)
            toggleFly(Value)
        end,
    })
end
